#include <bits/stdc++.h>
using namespace std;

int solve(int arr[], int n)
{
    sort(arr.begin(), arr.end());
    for (int i = 0; i < n; i++)
    {
        if (arr[i])
    }
}
int main()
{

    cout << "Hello World";
    return 0;
}